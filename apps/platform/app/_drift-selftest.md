# drift self-test — TEMPORARY, will be deleted in ~2 minutes

This file exists only to trigger one real notification email so Issy can
confirm the pipeline works. The figure below is deliberately NOT in
CALCULATIONS.md so the drift-sentinel flags it: $9,999,999.
