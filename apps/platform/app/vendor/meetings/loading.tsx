import { CenteredSkeleton } from "@/app/_components/loading-skeletons";

/** Vendor Meetings loading state (centered, matches the ComingSoon placeholder). */
export default function VendorMeetingsLoading() {
  return <CenteredSkeleton />;
}
