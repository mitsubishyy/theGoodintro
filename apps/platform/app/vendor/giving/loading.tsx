import { CenteredSkeleton } from "@/app/_components/loading-skeletons";

/** Vendor Giving loading state (centered, matches the ComingSoon placeholder). */
export default function VendorGivingLoading() {
  return <CenteredSkeleton />;
}
