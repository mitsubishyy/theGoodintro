import { CenteredSkeleton } from "@/app/_components/loading-skeletons";

/** Vendor Billing loading state (centered, matches the ComingSoon placeholder). */
export default function VendorBillingLoading() {
  return <CenteredSkeleton />;
}
