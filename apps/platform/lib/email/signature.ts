/**
 * Issy's email signature, appended to personal (from-Issy) emails so they read
 * like a normally typed message.
 *
 * PLACEHOLDER CONTENT: reconstructed from repo facts (name, role, workspace
 * address). Her real Gmail signature lives only in Gmail settings and is not
 * in the repo; replace these two constants with the real wording when she
 * provides it. Keep both parts in sync.
 */

export const SIGNATURE_HTML = [
  `Issy Hardwick<br>`,
  `Founder, TheGoodIntro<br>`,
  `issy@thegoodintros.com`,
].join("\n");

export const SIGNATURE_TEXT = ["Issy Hardwick", "Founder, TheGoodIntro", "issy@thegoodintros.com"].join(
  "\n",
);
